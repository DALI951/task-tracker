import 'package:flutter/material.dart';

class Brand {
  Brand._();

  static const String name = 'Task Tracker';
  static const String enterpriseName = 'Task Tracker';

  static const Color primary = Color(0xFFE53935);
  static const Color onPrimary = Colors.white;
  static const Color secondary = Color(0xFF1565C0);
  static const Color surface = Color(0xFFFAFAFA);
  static const Color cardBackground = Colors.white;

  static const Color done = Color(0xFF2E7D32);
  static const Color doing = Color(0xFF1565C0);
  static const Color pendingReview = Color(0xFF7B1FA2);
  static const Color pending = Color(0xFFE65100);
  static const Color problem = Color(0xFFC62828);

  static const double radiusSm = 8;
  static const double radiusMd = 12;
  static const double radiusLg = 16;

  static const EdgeInsets cardPadding = EdgeInsets.all(14);
  static const EdgeInsets screenPadding = EdgeInsets.all(16);
}
